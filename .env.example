# Copy this file to .env and fill in your credentials (never commit .env)

# Axon Server authentication token (used by docker-compose)
AXONIQ_PLATFORM_AUTHENTICATION=

# Axon Platform client credentials (used by the Spring Boot app)
AXONIQ_PLATFORM_CREDENTIALS=
